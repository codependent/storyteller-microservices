spring:
  application:
    name: hystrix-dashboard

server:
  port: 10000
  
turbine:
  appConfig: gatekeeper,random-story-microservice
  instanceUrlSuffix: /hystrix.stream
  clusterNameExpression: new String("default")