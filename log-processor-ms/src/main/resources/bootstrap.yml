---
spring:
  application:
    name: log-processor-ms
  profiles: lp1
  cloud:
    stream:
      bindings:
        rawLogMessages:
          group: logProcessor
    config:
      discovery:
        enabled: true
      failFast: true
      retry:
        maxAttempts: 100
        initialInterval: 2000
        maxInterval: 3000
        multiplier: 1.5
        
server:
  port: 14041

eureka:
  client:
    serviceUrl:
      defaultZone: http://archimedes1:8761/eureka/
---
spring:
  application:
    name: log-processor-ms
  profiles: lp2
  cloud:
    stream:
      bindings:
        rawLogMessages:
          group: logProcessor
    config:
      discovery:
        enabled: true
      failFast: true
      retry:
        maxAttempts: 100
        initialInterval: 2000
        maxInterval: 3000
        multiplier: 1.5
        
server:
  port: 41014

eureka:
  client:
    serviceUrl:
      defaultZone: http://archimedes2:8762/eureka/